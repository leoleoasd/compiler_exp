extern int printf(char *, ...);
int b = 2;
struct foo{
    int bar;
};
int main(int argc, char []* argv) {
    int t = 0;
    int _a = 0, b, c;
    int[5] a;
    struct foo test_struct;
    struct foo* test_pointer;
    *&*&t = 1 + 2 * 3 / 2 % 5 << 2;
    t = a[0] > 2 ? 1 : test_pointer->bar;
    t >>= 2;
    *(&t) = a[0] = t = t>=2;
    t = 1,2,3;
    return 0;
}
struct test {
    int a;
};
struct test_again{
    struct test* pointer;
    char***** b;
};
int foo(void) {
    struct test ss;
}